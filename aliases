# location commands
alias cdcr2='cd ~/pe/cr2rails'

# rails commands
alias ss='if [ -a script/server ]; then script/server; else rails s; fi'
alias sc='if [ -a script/console ]; then script/console; else rails c; fi'

# bundled commands
alias bspec='bundle exec spec'
alias bcuke='bundle exec cucumber'
